# 📋 User Flow: Сервис регистрации участников мероприятия по фото

## 🎯 Общая концепция

Система позволяет автоматически регистрировать участников на мероприятиях по их фотографиям с использованием технологии распознавания лиц. Участники заранее добавляются в базу данных с эталонными фото, после чего могут быстро регистрироваться на мероприятия через веб-камеру или загрузку фото.

---

## 👤 User Flow #1: Администратор добавляет участника

### Цель

Добавить нового участника в систему с его фотографией для последующего распознавания.

### Шаги

1. **Вход в систему**
   - Администратор переходит на `/login`
   - Вводит логин: `admin`, пароль: `admin` (по умолчанию)
   - Нажимает "Войти"
   - Система перенаправляет на `/admin`

2. **Создание участника**
   - В разделе "Создать участника" вводит:
     - **Логин** (уникальный идентификатор, обязательно)
     - **Имя** (ФИО участника, необязательно)
   - Загружает **фотографию** участника (форматы: JPG, PNG)
   - Видит превью загруженного фото

3. **Валидация фото**
   - Нажимает "Создать участника"
   - Система проверяет фото с помощью модуля `photo_capture.py`:
     - ✅ На фото должно быть **ровно одно лицо**
     - ❌ Если лиц нет или их больше одного → ошибка

4. **Сохранение в БД**
   - При успешной валидации:
     - Фото сохраняется в БД как BLOB
     - Создается запись в таблице `participants`
     - Показывается сообщение: "Участник создан"
   - При ошибке:
     - Показывается причина (нет лица, дубликат логина и т.д.)

5. **Просмотр участников**
   - В разделе "Участники" видны все добавленные люди
   - Отображаются: фото, ID, логин, имя

### Технические детали

- **Endpoint**: `POST /admin/add_participant`
- **Модуль фотографирования**: [photo_capture.py](photo_capture.py)
- **Проверка лица**: OpenCV Haar Cascade `haarcascade_frontalface_default.xml`
- **Хранение**: SQLite, таблица `participants`, фото в BLOB

---

## 🎪 User Flow #2: Администратор создает мероприятие

### Цель

Создать мероприятие, на которое будут регистрироваться участники.

### Шаги

1. **Вход в админ-панель**
   - Авторизация через `/login` (как в Flow #1)

2. **Создание мероприятия**
   - В разделе "Создать мероприятие" вводит название
   - Нажимает "Добавить событие"
   - Мероприятие сохраняется в таблице `events`
   - Появляется в списке доступных событий

### Технические детали

- **Endpoint**: `POST /admin/add_event`
- **База данных**: таблица `events`

---

## 📸 User Flow #3: Участник регистрируется через веб-камеру

### Цель

Зарегистрироваться на мероприятие, сделав фото с веб-камеры.

### Шаги

1. **Переход на страницу регистрации**
   - Участник открывает главную страницу `/`
   - Видит форму регистрации

2. **Выбор мероприятия**
   - В выпадающем списке выбирает нужное мероприятие
   - Вводит свое имя

3. **Включение камеры**
   - Переключается на режим "📷 Сделать фото"
   - Нажимает "🎥 Включить камеру"
   - Браузер запрашивает разрешение на доступ к камере
   - Участник разрешает доступ

4. **Захват фото**
   - Видео с камеры отображается на экране
   - Участник позиционирует лицо в кадре
   - Нажимает "📸 Сделать снимок"
   - Камера отключается, показывается превью снимка
   - При необходимости может нажать "🔄 Переснять"

5. **Регистрация**
   - Нажимает "✅ Зарегистрироваться"
   - Показывается "⏳ Идет проверка и регистрация..."

6. **Обработка фото**
   - Фото отправляется на сервер (`POST /register`)
   - Система проверяет наличие лица на фото
   - Модуль сравнения ([photo_compare.py](photo_compare.py)) сравнивает фото со всеми участниками в БД:
     - Использует комбинацию методов: SSIM, гистограммы, перцептуальный хэш, косинусное сходство
     - Вычисляет процент совпадения (0-100%)
     - Порог совпадения: **70%**

7. **Результаты сравнения**

   **Сценарий А: Успешная регистрация** ✅
   - Найден участник с совпадением ≥70%
   - Создается запись в таблице `attendance`
   - Показывается: "✅ Успешно зарегистрированы!"
   - Отображаются: логин, имя, процент совпадения

   **Сценарий Б: Повторная регистрация** ⚠️
   - Участник уже зарегистрирован на это мероприятие
   - Показывается: "⚠️ Вы уже зарегистрированы"
   - Отображается логин и процент совпадения

   **Сценарий В: Участник не найден** ❌
   - Ни один участник не совпал на ≥70%
   - Показывается: "❌ Вас нет в базе участников"
   - Если есть похожие → показывается ближайший кандидат
   - Совет: "Обратитесь к администратору"

   **Сценарий Г: Плохое фото** ❌
   - На фото нет лица или их несколько
   - Показывается: "❌ Фото не подходит"
   - Просьба переснять

### Технические детали

- **Frontend**: [user.html](templates/user.html), [user.js](static/user.js)
- **API камеры**: `navigator.mediaDevices.getUserMedia()`
- **Формат захвата**: JPEG, качество 90%
- **Endpoint**: `POST /register`
- **Модуль сравнения**: [photo_compare.py](photo_compare.py) → функция `compare()`
- **Порог совпадения**: 70%

---

## 💾 User Flow #4: Участник регистрируется загрузкой файла

### Цель

Зарегистрироваться на мероприятие, загрузив готовый файл с фото.

### Шаги

1. **Переход на страницу регистрации**
   - Участник открывает `/`

2. **Выбор мероприятия и имени**
   - Выбирает мероприятие из списка
   - Вводит свое имя

3. **Загрузка файла**
   - Переключается на режим "📁 Загрузить файл" (режим по умолчанию)
   - Нажимает "Выберите фото"
   - Выбирает файл с фото (JPG, PNG, WebP)
   - Видит превью выбранного файла

4. **Регистрация**
   - Нажимает "✅ Зарегистрироваться"
   - Далее процесс идентичен Flow #3 (шаги 5-7)

### Технические детали

- **Принимаемые форматы**: `.jpg`, `.jpeg`, `.png`, `.webp`
- **Остальное**: как в Flow #3

---

## 📊 User Flow #5: Администратор просматривает журнал посещений

### Цель

Посмотреть, кто зарегистрировался на мероприятия.

### Шаги

1. **Вход в админ-панель**
   - Авторизация через `/login`

2. **Просмотр журнала**
   - В разделе "Журнал посещения" нажимает "Обновить журнал"
   - Система загружает данные из таблицы `attendance`
   - Отображается JSON с данными:
     - Логин участника
     - Имя участника
     - Название мероприятия
     - Время регистрации
     - Процент совпадения (match_score)

### Технические детали

- **Endpoint**: `GET /admin/get_attendance`
- **Таблица**: `attendance` с JOIN к `participants` и `events`
- **Формат**: JSON-массив

---

## 📤 User Flow #6: Администратор экспортирует журнал в JSON

### Цель

Выгрузить данные о посещаемости в JSON-файл для передачи в другую систему.

### Шаги

1. **Вход в админ-панель**
   - Авторизация через `/login`

2. **Настройка фильтров** (опционально)
   - В разделе "Экспорт журнала в JSON" заполняет фильтры:
     - **Участник (ID)**: конкретный участник
     - **Мероприятие (ID)**: конкретное мероприятие
     - **Дата от**: начало диапазона (формат: YYYY-MM-DD)
     - **Дата до**: конец диапазона (формат: YYYY-MM-DD)
   - Если поля пустые → экспортируются все записи

3. **Экспорт**
   - Нажимает "Экспортировать в JSON"
   - Система создает файл в папке `tmp/`
   - Имя файла: `attendance_export_YYYYMMDD_HHMMSS.json`

4. **Результат**
   - Показывается сообщение:
     - ✓ Экспорт выполнен успешно
     - Имя файла
     - Путь к файлу
     - Количество записей

5. **Структура JSON-файла**

```json
{
  "export_date": "2026-02-13 15:30:45",
  "filters": {
    "participant_id": null,
    "event_id": 1,
    "date_from": "2026-02-01",
    "date_to": "2026-02-13"
  },
  "total_records": 15,
  "records": [
    {
      "id": 1,
      "participant": {
        "id": 5,
        "login": "ivan.petrov",
        "name": "Иван Петров"
      },
      "event": {
        "id": 1,
        "title": "Лекция по Machine Learning"
      },
      "timestamp": "2026-02-13 14:23:10",
      "match_score": 87.5
    }
  ]
}
```

### Технические детали

- **Endpoint**: `GET /admin/export_attendance`
- **Параметры**: `participant_id`, `event_id`, `date_from`, `date_to`
- **Формат**: JSON с метаданными
- **Место хранения**: `tmp/attendance_export_*.json`

---

## 🔧 Технические компоненты системы

### 1. Модуль фотографирования ([photo_capture.py](photo_capture.py))

- **Функция**: `validate_face(photo_path)`
- **Задача**: Проверка наличия ровно одного лица на фото
- **Технология**: OpenCV, Haar Cascade Classifier
- **Возвращает**: `True` если лицо одно, `False` если нет/много

### 2. Модуль сравнения ([photo_compare.py](photo_compare.py))

- **Класс**: `ImageComparator`
- **Методы сравнения**:
  - MSE (среднеквадратичная ошибка)
  - SSIM (структурное сходство)
  - Сравнение гистограмм цветов
  - Перцептуальный хэш (phash, dhash, whash)
  - Feature matching (ORB/SIFT)
  - Косинусное сходство
- **Функция**: `compare(path1, path2, threshold=70.0)`
- **Возвращает**: `(match: bool, score: float)`

### 3. База данных ([db.py](db.py))

- **СУБД**: SQLite
- **Таблицы**:
  - `admin` — администраторы
  - `participants` — участники (login, name, photo_blob)
  - `events` — мероприятия
  - `attendance` — журнал посещений

### 4. Веб-приложение ([app.py](app.py))

- **Фреймворк**: Flask
- **Endpoints**:
  - `/` — пользовательская страница
  - `/login` — вход администратора
  - `/admin` — панель администратора
  - `/register` — регистрация участника
  - `/admin/add_participant` — добавление участника
  - `/admin/add_event` — создание мероприятия
  - `/admin/get_attendance` — просмотр журнала
  - `/admin/export_attendance` — экспорт в JSON

### 5. Интерфейсы

- **Администратор**: [admin.html](templates/admin.html), [admin.js](static/admin.js)
- **Пользователь**: [user.html](templates/user.html), [user.js](static/user.js)
- **Стили**: [style.css](static/style.css)

---

## ✅ Соответствие ТЗ

| Требование                      | Статус | Реализация                                                       |
| ------------------------------- | ------ | ---------------------------------------------------------------- |
| Модуль фотографирования         | ✅     | [photo_capture.py](photo_capture.py) + веб-камера в UI           |
| Модуль сравнения 2-х фотографий | ✅     | [photo_compare.py](photo_compare.py) — 7 методов                 |
| Интерфейс администратора        | ✅     | [admin.html](templates/admin.html) + [admin.js](static/admin.js) |
| Интерфейс пользователя          | ✅     | [user.html](templates/user.html) + [user.js](static/user.js)     |
| Создание и наполнение БД        | ✅     | [db.py](db.py) — `init_db()`                                     |
| Внесение фото в БД              | ✅     | `/admin/add_participant`                                         |
| Регистрация в журнале           | ✅     | Таблица `attendance`                                             |
| Выгрузка в JSON с фильтрами     | ✅     | `/admin/export_attendance`                                       |

---

## 🚀 Быстрый старт

### Установка зависимостей

```bash
pip install -r requirements.txt
```

### Запуск сервера

```bash
python app.py
```

### Первое использование

1. Откройте http://127.0.0.1:5000/login
2. Войдите: admin / admin
3. Добавьте участников с фото
4. Создайте мероприятие
5. Откройте http://127.0.0.1:5000/
6. Зарегистрируйтесь через камеру!

---

## 📱 Поддержка устройств

- ✅ **Desktop** — полная функциональность
- ✅ **Мобильные устройства** — работает веб-камера (фронтальная)
- ✅ **Адаптивный дизайн** — медиа-запросы для экранов <600px

---

## 🔐 Безопасность

- ⚠️ **Демо-версия**: пароль admin/admin
- ⚠️ **Для продакшена**: изменить пароли, добавить хеширование (bcrypt)
- ✅ **Фото в БД**: хранятся как BLOB, не в файловой системе
- ✅ **Проверка типов файлов**: только JPG/PNG/WebP

---

## 📝 Дополнительные возможности

- **Множественные администраторы**: добавить записи в таблицу `admin`
- **История попыток**: логировать все попытки регистрации
- **Статистика**: дашборд с графиками посещаемости
- **Email-уведомления**: отправка участникам подтверждений
- **API**: REST API для интеграции с другими системами

---

**Сервис готов к использованию! 🎉**
